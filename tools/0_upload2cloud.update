#!/bin/sh

## hyphop ##

# just run as
#	./tools/*

case "$PWD" in
    *upload2cloud)
    ;;
    *)
    echo "[e] run only as ./tools/*" >&2
    exit 0
esac


echo "[i] UPDATE" >&2
for t in			\
	upload2cloud		\
	upload2cloud-yandex	\

do

echo "[i] update $t" >&2

./$t --help > $t.help.md

done

./tools/*_push